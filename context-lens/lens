#!/usr/bin/env python3
"""lens — one command to index, explore, and query your codebase."""

import sys
from pathlib import Path

# Resolve to the src directory regardless of where lens is called from
src_dir = Path(__file__).resolve().parent / "src"
sys.path.insert(0, str(src_dir))

from cli import main

main()
